# Project Decisions

Architectural and design decisions made during development.
All agents should read this before starting work and log new decisions here.

## New Download dialog: video-host page URLs now routed through the extraction registry, gated by a host allowlist
**When**: 2026-08-14

TASK-265. A pasted YouTube / X(twitter.com) / Facebook / Instagram *page* URL used to short-circuit straight to the plain IResourceProbe path (NewDownloadViewModel.TryDetectMediaAsync only consulted IMediaExtractorRegistry when LooksLikeMediaManifest(uri) — an .m3u8/.m3u/.mpd), so the per-extractor decline/failure reasons built in b52e1b9 were never seen for those URLs and extraction failures were unexplained.

Fix: the gate is now `LooksLikeMediaManifest(uri) || MediaHosts.IsKnownVideoHost(uri)`. New internal `JustDownload.Core/Media/Extraction/MediaHosts.cs` exposes `IsKnownVideoHost(Uri)` — a coarse `OrdinalIgnoreCase` host allowlist covering youtube.com/youtu.be (+ subdomains), x.com/twitter.com (+ subdomains), facebook.com/fb.watch (+ subdomains), instagram.com (+ subdomains) — mirroring the browser extension's `EXTRACTABLE_HOSTS` (extended for x.com in c421dec and instagram.com in d768653). The per-extractor `LooksLikeX` methods remain the precise recognizers; the allowlist is only the "should we even try" gate. For a video-host page URL the registry runs; if no extractor returns a `MediaSource`, `extraction.Source` is null and the existing `_mediaFailure = MediaExtractionMessage.TryDescribeFailure(uri, extraction.Attempts)` branch (NewDownloadViewModel.cs:425) maps the attempts — including a yt-dlp "Sign in to confirm you're not a bot" failure — into `UrlWarning`. No new explanation code added.

The allowlist is MANDATORY, not a nicety: `YtDlpMediaExtractor` is a catch-all with `Priority = int.MaxValue` and no host check, so it would spawn `yt-dlp --dump-json` for *any* URL handed to the registry. Routing an arbitrary pasted file URL to the registry would therefore launch yt-dlp on every plain-file paste, breaching AC2 ("no heavier extractor ever runs against a non-manifest link"). A plain non-video-host URL (e.g. example.com/file.zip) still does NOT call `ExtractAsync` — proven by a regression test asserting `DidNotReceive`.

`MediaHosts` is `internal`; because it's consumed by `JustDownload.App`, `JustDownload.Core.csproj` now has `<InternalsVisibleTo Include="JustDownload.Tests" />` joined by a new `<InternalsVisibleTo Include="JustDownload.App" />` (Tests already had access). No interface change (`IMediaExtractor.CanHandle` was explicitly not added) — surgical.

**Impact**: JustDownload.Core/Media/Extraction/MediaHosts.cs (new), JustDownload.Core/JustDownload.Core.csproj (InternalsVisibleTo App), JustDownload.App/ViewModels/NewDownloadViewModel.cs (gate + doc comment), JustDownload.Tests/App/NewDownloadViewModelTests.cs (5 new tests: YouTube/X/Facebook page URLs consult the registry; plain non-video-host URL does not; failed attempt on a video-host URL surfaces the attributed reason, not the generic "couldn't find"). 54/54 NewDownloadViewModelTests green.

## Media extractors: suggested filename from real title, not opaque id
**When**: 2026-08-13 18:24:18

User-reported UX bug: downloaded videos were saved as "ytdlp-{id}"/"youtube-{id}" instead of the real video title. Fixed by reading yt-dlp's "title" field (YtDlpMediaExtractor) and videoDetails.title from the already-parsed player response (YouTubeMediaExtractor), sanitized via a new shared CrossPlatformFileName.Sanitize(string?) helper (same logic HttpFileNameResolver already used privately, now reusable). Both extractors fall back to the old opaque id-based name when no title is available/parseable — never throws, matches the existing graceful-degradation contract (D3). FacebookMediaExtractor was intentionally left unchanged: its embed page doesn't reliably expose a title without an extra page fetch — flagged as a follow-up, not done in this pass.

**Impact**: JustDownload.Core/CrossPlatformFileName.cs (new public Sanitize method), JustDownload.Core/Media/YtDlp/YtDlpMediaExtractor.cs, JustDownload.Core/Media/YouTube/YouTubeMediaExtractor.cs, and their tests. Any future extractor wanting a title-based filename should call CrossPlatformFileName.Sanitize(title) with an id-based fallback, following this same pattern.

## Facebook title follow-up resolved: og:title now used for suggested filename
**When**: 2026-08-13 18:33:52

Follow-up to "Media extractors: suggested filename from real title, not opaque id" (2026-08-13), which explicitly deferred FacebookMediaExtractor because its embed page "doesn't reliably expose a title without an extra page fetch". Investigated further: Facebook's VideoInlinePlayerConfig JSON (the source of hd_src/sd_src) has no title field at all in either the fixtures or real-world extractor references (yt-dlp's own FacebookIE reads title from og:title/twitter:title meta tags or the <title> element, never from that JSON blob) — so a JSON field was never going to be an option. The embed endpoint's own <title> element is generic ("Facebook embed", confirmed in the existing facebook-embed-real.html fixture) and not the video's title, so it is deliberately NOT used as a source. Implemented: read the video title from the og:title meta tag — checked on embedHtml first (always fetched), then on pageHtml (only present when a short link was already followed to resolve the video id) — using a new OgTitleRegex, HTML-decoded via System.Net.WebUtility.HtmlDecode, then CrossPlatformFileName.Sanitize(title) with fallback to the existing facebook-{videoId} name, matching the exact pattern YouTubeMediaExtractor/YtDlpMediaExtractor already use. No extra HTTP fetch was needed — both HTML sources checked were already being fetched for hd_src/sd_src or video_id resolution. Never throws (TryGetTitle degrades to null on no match), matching D3's graceful-degradation contract.

**Impact**: JustDownload.Core/Media/Facebook/FacebookMediaExtractor.cs (pageHtml hoisted out of the short-link branch so it's available as a secondary title source; new TryGetTitle + OgTitleRegex; SuggestedFileName now sanitized-title-or-fallback). JustDownload.Tests/Media/FacebookMediaExtractorTests.cs (two new tests: title found -> sanitized title used; no og:title anywhere -> falls back to facebook-{id}). JustDownload.Tests/Fixtures/facebook-embed-with-title.html (new synthetic fixture with an og:title tag). All extractors flagged in the original entry (YouTube, yt-dlp, Facebook) now use the real-title-with-fallback pattern; no further follow-up remains from that decision.

## HLS/DASH manifest URLs in New Download dialog now detected via IMediaExtractorRegistry, gated by URL extension
**When**: 2026-08-13 19:22:24

Bug fix (Twitter/X .m3u8 sniffed by the extension, and any other host serving a sniffable .m3u8/.mpd, downloaded as raw manifest text instead of a real video): NewDownloadViewModel.DetectAsync previously only ever ran IResourceProbe's plain HTTP probe, so an HLS/DASH manifest URL was treated as if it were the video file itself — EnqueueDownloadRequest.MediaKind stayed null, and DownloadManager.cs:223 took the plain progressive HTTP-GET path, saving the playlist/manifest text verbatim.

Fix: DetectAsync now first checks the URL's file extension (.m3u8/.m3u/.mpd, case-insensitive) via a new LooksLikeMediaManifest() prefilter. Only when it matches does it call the existing IMediaExtractorRegistry.ExtractAsync (the same registry/extractors MediaVariantPickerViewModel's extension-driven quality picker already uses — HlsMediaExtractor, DashMediaExtractor, etc.) — reusing the real playlist/manifest parsing rather than duplicating it. Every other URL (the overwhelming majority pasted/typed into this dialog) never reaches the registry at all, so: (a) ordinary progressive downloads are byte-for-byte unaffected (proven by a new regression test asserting the registry is never consulted for a non-manifest URL), and (b) YouTube/Facebook/yt-dlp extractors (also registered in the same registry, priority-ordered before the catch-all) never run against a plain pasted link — they only see manifest-shaped URLs, where they cheaply decline on host mismatch before HLS/DASH's real extractor takes over.

Considered but rejected: (1) Content-Type-based detection in addition to extension — would need IResourceProbe/ResourceProbeResult changed to expose Content-Type, a bigger surface than the reported bug needs; extension-based detection alone covers the real-world case (Twitter/X CDN URLs carry .m3u8 in the path) — flagged as a follow-up if a manifest URL without a recognizable extension is ever reported. (2) Always calling the full registry for every URL (no prefilter) — rejected for being needlessly heavy (D9 "light & fast") and for risking the optional yt-dlp fallback extractor spawning/considering a subprocess on URLs that were never meant to be media at all. (3) Wiring ITosNoticeGate into this new extraction call site (its own interface doc says it must be "consulted right before extraction actually runs") — NOT done in this fix to keep the change surgical and scoped to the reported routing bug; this is a known, flagged gap: a media download can now be enqueued through the New Download dialog without the one-time ToS notice CLAUDE.md §5 calls for. Recommended as a fast follow-up.

For a detected master playlist/adaptive manifest (Variants.Count > 0), the same selection helpers the quality picker uses are reused to resolve a concrete stream URL up front (no picker UI exists in this dialog): VideoQualitySelector.Select(..., AppSettings.DefaultVideoQuality) for video, AudioQualitySelector.Select(...) for audio when present, AppSettings.DefaultContainer for the output container. This is required, not optional: JustDownload.Core/Media/Hls/HlsDownloader.cs:50-54 throws HlsExtractionException if ever given a master (not media) playlist URL directly, so routing the raw master URL through as MediaKind.Hls without pre-resolving a variant would just trade one failure mode (raw text saved) for another (download always fails).

**Impact**: JustDownload.App/ViewModels/NewDownloadViewModel.cs (new IMediaExtractorRegistry constructor dependency; DetectAsync tries media detection before the plain probe; new ApplyMediaDetection/TryDetectMediaAsync/LooksLikeMediaManifest/MediaFileName helpers; SubmitAsync sets MediaKind/MediaAudioUrl/MediaContainer and uses the resolved variant Url when a media source was detected; OnUrlChanged now also clears the stale detection). JustDownload.Tests/App/NewDownloadViewModelTests.cs (Harness gained MediaRegistry + SetMediaSource; two new tests: HLS master-playlist detection/routing, and a plain-URL regression proving the registry is never consulted). JustDownload.Tests/App/NewDownloadWindowTests.cs (constructor call site updated for the new parameter). MediaVariantPickerViewModel and the extension's EXTRACTABLE_HOSTS list were not touched — the extension-driven extract=true flow is unaffected (verified: MediaVariantPickerTests, 29/29, still green).

## Diagnostics: live/opt-in-only probes are separate console projects, not xUnit tests
**When**: 2026-08-13 20:04:41

Task 57590d08 needed a harness that hits real external sites (facebook.com, x.com, youtube.com, instagram.com) using the real DI-wired IMediaExtractor set, but must never run automatically in dotnet build/dotnet test/CI. Followed the existing JustDownload.Perf precedent: a plain console-app project (OutputType Exe, top-level Program.cs, no [Fact]/[Trait]) that IS part of JustDownload.sln and builds cleanly under `dotnet build -c Release`, but only executes when explicitly invoked via `dotnet run --project <name>`. This was chosen over an xUnit [Trait("Category","LiveSmoke")] approach because the project already has this exact pattern established (Perf), it needs no test-filter discipline from every future `dotnet test` invocation to stay silent, and a console app's stdout is a more readable diagnostic report than xUnit's test-runner output for a human reading per-extractor accept/decline/exception lines.

**Impact**: New project JustDownload.LiveSmoke (references JustDownload.Core only). Future "hits real external systems, must never run in CI" diagnostic needs should follow the same shape: a console project added to the .sln, built by default, run only via an explicit `dotnet run --project`.

## New Download dialog: detection state is replaced, never pre-cleared
**When**: 2026-08-13 20:37:04

NewDownloadViewModel.DetectAsync used to wipe _detectedMedia at the top of every pass, before any await. Clicking "Download now" moves focus off the URL TextBox, and NewDownloadWindow.OnUrlCommitted (LostFocus) fires a fresh DetectAsync in the same click — that pass wiped the already-resolved HLS source and was still awaiting the extractor when SubmitAsync read it, so the request silently degraded to a plain progressive GET of the .m3u8 (DB rows 60/62/63: media_kind NULL, 334-byte "video"). The rule is now: _detectedMedia is only ever REPLACED by the pass that produces a new verdict (ApplyMediaDetection sets it, ApplyDetection nulls it) or dropped when the URL itself changes (OnUrlChanged, which now also clears DetectionMessage so the two can't disagree). A cancelled or superseded pass leaves the previous good result intact. Paired with a guard in SubmitAsync: a LooksLikeMediaManifest URL with no _detectedMedia re-runs detection once and, if it still can't resolve, refuses the submit with a warning instead of enqueuing the manifest as a file (§5 no silent failures). Alternative considered and rejected: snapshotting detection state into the submit path or gating submit on !IsDetecting — both leave the underlying "a superseded pass can destroy good state" hazard in place for the next caller.

**Impact**: JustDownload.App/ViewModels/NewDownloadViewModel.cs (DetectAsync, ApplyDetection, SubmitAsync, OnUrlChanged). Any future async state applied by DetectAsync should follow the same replace-don't-pre-clear rule. Covered by 3 tests in JustDownload.Tests/App/NewDownloadViewModelTests.cs.

## HLS: #EXT-X-MAP initialization segment is part of the download, not metadata
**When**: 2026-08-13 21:01:31

Hands-on diagnosis of the user's "Twitter video downloads but won't play" report found M3U8Parser.ParseMedia silently ignored #EXT-X-MAP (it starts with '#' and matched no known tag, so it fell through the else-if chain). Twitter/X — and every CMAF/fragmented-MP4 HLS stream — carries the ftyp/moov boxes ONLY in that initialization segment and serves .m4s fragments that are meaningless without it. HlsDownloader therefore fetched just the fragments and HlsConcatenator produced a file starting with styp/moof; ffprobe rejects it with "trun track id unknown, no tfhd was found". Verified on the user's own machine: the app's output for the reported tweet was 851149 bytes and unplayable; prepending the 898-byte init segment to the byte-identical fragments yields a valid 69.88s H.264 file (852047 bytes, ffprobe clean), and the real DI-wired IHlsDownloader+IHlsConcatenator now produce exactly that. Design choices: (a) only the FIRST #EXT-X-MAP is kept — a second, mid-stream map re-initialises the decoder, which a pure byte-append pipeline cannot express, and appending it would corrupt the output; (b) the init segment is fetched BEFORE the parallel segment loop so an unfetchable init fails fast with HlsExtractionException rather than silently returning fragments alone (CLAUDE.md §5, no silent failures); (c) InitializationSegment was added as a defaulted positional record parameter so the single existing construction site needed no churn. Rejected: treating the init segment as a synthetic HlsSegment (it has no duration, no media sequence, and must never be AES-decrypted) and post-processing with ffmpeg (a byte prepend is exact and needs no external process).

**Impact**: JustDownload.Core/Media/Hls/M3U8Parser.cs (MapTag const, ParseMap helper), HlsMediaPlaylist.cs (new InitializationSegment property), HlsDownloader.cs (fetch + prepend to SegmentFiles, init bytes counted in TotalBytes). Any future HLS work must keep the init segment first in SegmentFiles — the concatenator is a pure ordered byte append and order is load-bearing. NOTE: #EXT-X-BYTERANGE remains unsupported for both EXT-X-MAP and media segments (pre-existing gap, not introduced here) — a playlist using byte-range addressing will still produce a wrong file. Covered by 4 tests in JustDownload.Tests/Media/{M3U8ParserTests,HlsDownloaderTests}.cs.

## Media extraction reports per-extractor outcomes; "recognised but failed" is signalled by MediaExtractionFailedException
**When**: 2026-08-13 21:17:57

Every extractor returned a bare null and MediaExtractorRegistry.ExtractAsync caught-and-continued, so a DNS failure, a yt-dlp bot challenge and "this page genuinely has no video" all collapsed into the one string "Couldn't find downloadable media at this URL." in the picker. On Sarfraz's machine the only way to recover a real reason was running yt-dlp by hand — YtDlpMediaExtractor logged its stderr at LogLevel.Debug and his logging.minimum_level is Error, so it was discarded. The reason therefore had to travel back through the return path, not the log.

TWO contract changes were weighed. (a) Change IMediaExtractor.TryExtractAsync to return a rich attempt type: cleanest on paper but touches all 6 extractors, every `return null`/`return new MediaSource` site, LiveSmoke and ~40 extractor tests. (b) Registry-level only: cannot express "recognised the URL but failed" at all, because that case currently returns null exactly like a decline. CHOSEN: a middle route — the registry's return type changes (IMediaExtractorRegistry.ExtractAsync now returns a non-null MediaExtractionResult { Source, Attempts }), while IMediaExtractor's signature is untouched; an extractor that recognised the URL but could not extract throws the new sealed MediaExtractionFailedException carrying the reason. That keeps declines cheap and silent (return null, unchanged for 5 of 6 extractors), needs no churn in extractors that already behave correctly, and is not exceptions-as-control-flow: it IS a failure.

Vocabulary: MediaExtractionOutcome { Accepted, Declined, Failed, NetworkFailure }. NetworkFailure is its own category because HttpRequestException / SocketException / a non-user-cancelled OperationCanceledException (an extractor's internal timeout) say nothing about whether media exists — reporting them as "no media found" is what sent the user hunting for a broken extractor instead of his NextDNS resolver null-routing facebook.com. Detection walks InnerException, since a DNS failure arrives as HttpRequestException wrapping SocketException. Routing is deliberately UNCHANGED: first non-null still wins, priority order untouched, one failing extractor still does not abort the chain.

MediaExtractionAttempt has a private constructor and four static factories, so Reason is non-null exactly for Failed/NetworkFailure and null otherwise — the illegal pairings are unrepresentable rather than validated. Both factories run the reason through ExtractionReasonSanitizer, which collapses whitespace, strips the query/fragment off every embedded URL (where signatures/tokens/cookies live) and truncates at 160 chars. Sanitizing at construction, not at display, means an unredacted reason can never exist in an attempt at all (CLAUDE.md §5) — including in the log line the registry writes for it.

D5 is preserved: Core produces structured outcomes only. The wording lives in JustDownload.App/Formatting/MediaExtractionMessage.cs, which both dialogs call — network failure wins over everything, then attributed extractor reasons (max 2), then the original generic string when every extractor merely declined.

YtDlpMediaExtractor's two non-failure gates (master toggle off, yt-dlp not provisioned) still decline silently. Its four real failure modes — non-zero exit, launch failure, unreadable JSON, no usable format — now throw with the reason; for a non-zero exit that is the LAST "ERROR: …" line of stderr with the prefix stripped, which is where yt-dlp puts the actual cause (bot challenge, HTTP 429, missing JS runtime). Deliberately NOT special-cased: "Unsupported URL" is reported as a failure like any other rather than silently downgraded to a decline — a magic-string test risked mis-classifying, and "yt-dlp: Unsupported URL" is more useful to the user than the generic message anyway.

**Impact**: NEW in JustDownload.Core/Media/Extraction/: MediaExtractionOutcome.cs, MediaExtractionAttempt.cs, MediaExtractionResult.cs, MediaExtractionFailedException.cs, ExtractionReasonSanitizer.cs. CHANGED: IMediaExtractorRegistry.ExtractAsync now returns Task<MediaExtractionResult> (never null) — every call site reads .Source; MediaExtractorRegistry classifies each attempt; IMediaExtractor's doc contract now names MediaExtractionFailedException; YtDlpMediaExtractor throws instead of returning null for its four failure modes. NEW JustDownload.App/Formatting/MediaExtractionMessage.cs; MediaVariantPickerViewModel.LoadCoreAsync and NewDownloadViewModel (DetectAsync / TryDetectMediaAsync / SubmitAsync's manifest refusal, via the new _mediaFailure field) use it. JustDownload.LiveSmoke/Program.cs handles the new exception. ANY FUTURE EXTRACTOR: return null only for "not mine"; throw MediaExtractionFailedException with a reason whenever you recognised the URL and could not deliver — returning null there is now a silent failure. Tests: MediaExtractorRegistryTests, YtDlpMediaExtractorTests, MediaVariantPickerTests, NewDownloadViewModelTests, DashLoopbackTests, ExtractorCorpusTests, TestFixturesTests.

## HLS: #EXT-X-BYTERANGE resolved at parse time; a Range-ignoring server is sliced locally, never appended whole
**When**: 2026-08-13 21:27:02

RFC 8216 §4.3.2.2 lets #EXT-X-BYTERANGE omit its offset, in which case the sub-range continues at the byte after the previous sub-range of the SAME resource URI. That carry-forward is resolved inside M3U8Parser, so the model (HlsByteRange(Length, Offset), nullable on HlsSegment and on the new HlsInitializationSegment) never carries a missing offset — illegal states are unrepresentable downstream. A byte range that cannot be resolved (offset omitted with no prior sub-range of that URI, zero/negative/non-numeric length, invalid offset) throws HlsExtractionException from ParseMedia rather than being dropped: dropping it would fetch the whole resource per segment and silently emit a wrong file. This makes ParseMedia throwing (it previously only threw on null arguments); HlsDownloader already lets HlsExtractionException propagate.

On download, a ranged segment is fetched with Range: bytes=<offset>-<offset+length-1>. Two server behaviours are handled explicitly: a 206 body whose length differs from the requested length is an error (fail loudly, naming the URI); a 200 means the server ignored Range and sent the whole resource, in which case the requested window is SLICED locally (with a warning log) instead of failing. Slicing was chosen over failing because it yields the byte-correct output — the alternative, appending the full body, is the silent-wrong-output class of bug — while a hard failure would break downloads that are otherwise perfectly recoverable. It is still guarded: if the returned body is too short to contain the sub-range, that throws HlsExtractionException naming the URI. The cost is wasted bandwidth (the resource is re-fetched per segment on such servers), which is accepted for correctness.

EXT-X-MAP's BYTERANGE attribute uses the same resolver and shares the same per-resource carry-forward table, so #EXT-X-MAP:URI="x.mp4",BYTERANGE="800@0" followed by an offset-less segment range on x.mp4 resolves correctly.

**Impact**: JustDownload.Core/Media/Hls/: new HlsByteRange.cs and HlsInitializationSegment.cs; HlsSegment gains an optional ByteRange; HlsMediaPlaylist.InitializationSegment changes type from Uri? to HlsInitializationSegment? (only consumers were HlsDownloader and the parser tests); M3U8Parser.ParseMedia can now throw HlsExtractionException; HlsDownloader.FetchBytesAsync takes an HlsByteRange? (CancellationToken stays last). Tests: MapTransport now honours Range with 206 + Content-Range and has an IgnoreRangeRequests switch for the adversarial case, and records RequestedRanges. Playlists without byte ranges are unaffected — no Range header is sent (asserted by a regression test).

## ToS notice for the New Download dialog is gated at submit time, not inside DetectAsync
**When**: 2026-08-13 21:31:48

Closes the gap flagged as a follow-up in "HLS/DASH manifest URLs in New Download dialog now detected via IMediaExtractorRegistry" (2026-08-13): that fix added an IMediaExtractorRegistry.ExtractAsync call site in NewDownloadViewModel.DetectAsync without consulting ITosNoticeGate, so an extraction-backed HLS/DASH download could be enqueued from the New Download dialog with the one-time "may violate site ToS" notice (CLAUDE.md §5, docs/LEGAL.md) never shown.

Placement: NewDownloadViewModel.SubmitAsync consults _tosGate.ConfirmAsync() immediately before building/enqueuing a request, and only when _detectedMedia is non-null (i.e. the request will carry a non-null MediaKind). It is deliberately NOT consulted inside DetectAsync, which is where MediaVariantPickerViewModel.LoadCoreAsync:307 puts it. Reason: the picker is opened deliberately by the user, whereas DetectAsync runs automatically — debounced on every keystroke in the URL box and again on the box's LostFocus (NewDownloadWindow.axaml.cs:35). Gating there would raise a modal legal dialog mid-typing and re-raise it on every subsequent detect pass if the user cancelled. §5 requires the notice for the media *download*; submitting is the act of consent, detection is passive metadata probing. Checking before BuildProxyOverrideAsync also avoids touching the keychain for a download that is about to be cancelled.

On decline: nothing is enqueued, nothing is started, the dialog stays open and UrlWarning explains the cancellation — mirroring the existing "this link is a streaming playlist" refusal rather than silently closing. A plain (non-manifest) URL never reaches the gate at all.

The gate is a required, non-nullable constructor parameter with ArgumentNullException.ThrowIfNull, matching MediaVariantPickerViewModel — an optional/nullable gate would let a mis-wired host silently skip the notice. DI already resolves it (App.axaml.cs:50 registers ITosNoticeGate; :70 registers NewDownloadViewModel as transient), so no App.axaml.cs change was needed; only the two test construction sites were updated.

ITosNoticeGate's own doc comment said it must be "consulted right before extraction actually runs"; it was updated to state the real contract — consulted at the point the user commits to media, i.e. before extraction (picker) OR before enqueueing an extraction-backed download (New Download dialog), never for plain HTTP/FTP.

**Impact**: JustDownload.App/ViewModels/NewDownloadViewModel.cs (new ITosNoticeGate constructor dependency; gate check at the top of the enqueue path in SubmitAsync). JustDownload.App/Services/ITosNoticeGate.cs (doc comment only). JustDownload.Tests/App/NewDownloadViewModelTests.cs (Harness gained a TosGate substitute that accepts by default, plus a shared HlsSource helper; three new tests: declined → no enqueue/no start/dialog stays open, accepted → request enqueued with MediaKind/MediaAudioUrl/MediaContainer intact, plain URL → gate never consulted). JustDownload.Tests/App/NewDownloadWindowTests.cs (constructor call site). MediaVariantPickerViewModel's existing gate behaviour and JustDownload.Core are untouched. Any future UI surface that can enqueue an extraction-backed download must consult the gate at its own commit point.

## Extension: x.com/twitter.com route through extraction, with a sniffed-stream fallback carried over IPC; sniffer prefers the HLS master playlist
**When**: 2026-08-13 21:49:19

Two coupled changes to the MV3 extension's routing/sniffer path (TASK-241).

(A) x.com and twitter.com added to EXTRACTABLE_HOSTS in extension/src/jdcore.js, so a status page is handed to the app's extractor pipeline instead of a guessed stream URL. Verified hands-on that yt-dlp resolves https://x.com/unicodef1wn/status/2087461469881336049 into a real title plus 4 HLS variants. But there is NO in-house Twitter extractor in JustDownload.Core/Media/, so on an app where yt-dlp is not enabled/provisioned the page is declined by every extractor — which would have been a straight regression for those users, who previously got a working (if low-quality) sniffed .m3u8 download.

Fallback design considered and rejected:
- Extension-only Shift+click on the icon: ~10 lines, but effectively invisible; a non-yt-dlp user still hits "couldn't find downloadable media" first.
- A second floating icon: discoverable, but adds clutter on YouTube/Facebook and needs its own retry loop because the sniffer often has nothing at attach time.
- Overloading an existing field (e.g. `referrer`, which the extract path currently drops) to smuggle the stream URL: rejected as a hidden contract abuse.
- Dropping item A entirely: safe, but loses the real video title yt-dlp gives.

Chosen: a genuine optional field on the existing native-message contract. `fallbackUrl` flows extension -> ExtensionMessageHandler.AcceptDownloadAsync -> PendingLink.FallbackUrl -> BrowserLinkHandoff.FallbackUrl -> MediaVariantPickerViewModel.FallbackUrl. When extraction of the page finds nothing, the picker sets CanUseFallback and shows one button ("Use the stream your browser detected") which swaps Url to the sniffed stream and re-extracts, reusing the existing RedetectAsync path. Never offered for the URL that just failed, so it cannot loop. This required editing JustDownload.Core (PendingLink + one ReadString line) because the whole native-message schema is parsed there — JustDownload.NativeHost is only a bootstrapper and cannot see the message body. Explicitly signed off by Sarfraz before implementing.

The extension resolves the fallback at CLICK time, not at icon-attach time: by the time a human clicks, the player has been fetching for seconds, whereas the sniffer usually has nothing the instant the <video> element appears. This also avoids adding a second retry loop.

(B) The sniffer now hands over the HLS MASTER (multivariant) playlist rather than the currently-playing variant. Previously the newest sniffed .m3u8 won, which is why a default_video_quality=P720 setting still produced a 396x270 file and left the quality picker one fixed rendition. Detection is a body check, not a URL-shape guess: only a master carries #EXT-X-STREAM-INF, and variant-playlist URL shapes differ per CDN (Twitter uses /pl/ for masters and /vid/avc1/<WxH>/ for variants, others do not). background.js fetches candidate playlists — allowed without new permissions, manifest.base.json already declares host_permissions <all_urls>, and only the background worker escapes page CORS — and additionally requires the master to actually LIST the playing variant, so a timeline page with several videos cannot hand over the wrong video's master. Results are memoized per URL (content.js polls GET_TAB_MEDIA on a bounded retry loop) and dropped with the tab's media. Every uncertain path (no master observed, fetch failed, non-HLS) degrades to today's behaviour — the variant — never to nothing.

GET_TAB_MEDIA's reply gained a `preferred` field; the background now decides which detection to use and content.js just consumes it, removing the duplicate audio-filtering/most-recent logic that lived in content.js's sniffedVideoUrl().

**Impact**: extension/src/jdcore.js (EXTRACTABLE_HOSTS, buildDownloadMessage.fallbackUrl, new pure parseMasterVariants/playlistTargets), extension/src/background.js (masterVariantsOf + preferredMedia + forgetTab, GET_TAB_MEDIA now returns `preferred`, sendDownload takes fallbackUrl), extension/src/content.js (sendHandoff resolves the fallback at click time; sniffedVideoUrl now trusts the background's choice). JustDownload.Core/NativeMessaging/PendingLink.cs + ExtensionMessageHandler.cs (one optional field). JustDownload.App: BrowserLinkHandoff, App.axaml.cs handoff routing, MediaVariantPickerViewModel (FallbackUrl/CanUseFallback/UseFallbackAsync), MediaVariantPickerWindow.axaml(.cs). Tests: extension/test/master-playlist.test.js (new), jdcore.test.js, content.test.js (3 existing extract-click tests rewritten for the now-async hand-off), JustDownload.Tests/App/MediaVariantPickerTests.cs, JustDownload.Tests/NativeMessaging/ExtensionMessageHandlerTests.cs. Any future extension->app field must be added in Core's ExtensionMessageHandler/PendingLink — NativeHost cannot intercept the message body.

## yt-dlp cookie auto-fallback for YouTube bot-detection (TASK-...)
**When**: 2026-08-14

YouTube frequently returns the "Sign in to confirm you're not a bot" wall, HTTP 429, or an explicit "use cookies" hint. yt-dlp 2026.06.09 (the bundled build) supports two cookie mechanisms: `--cookies FILE` (Netscape-format cookie file) and `--cookies-from-browser BROWSER` (live cookie store of chrome/firefox/brave/etc.). Both are investigated and supported.

A new setting pair was added to `AppSettings` (null defaults, so behaviour is byte-identical today when unset): `YtDlpCookieFilePath` (key `media.video_capture_cookie_file`) and `YtDlpCookieBrowser` (key `media.video_capture_cookie_browser`), wired through `SettingsSerializer` (ToStorage/FromStorage). The cookie FILE is the recommended/preferred fallback — a self-contained, portable file the user exports once and points at; the browser variant is secondary (requires the browser installed and, on some OSes, a keyring to decrypt cookies).

Retry logic in `YtDlpMediaExtractor.TryExtractAsync`: the base probe runs exactly as before (no cookie argv). On a thrown `MediaExtractionFailedException` whose Reason matches a bot-detection substring (case-insensitive: "confirm you're not a bot", "429", "too many requests", "cookies", "bot") AND a cookie source is configured, the probe is RETRIED ONCE with the cookie args appended — `--cookies <path>` then `--cookies-from-browser <browser>`, in that order, both only when set. The retry propagates whatever it returns or throws (success → result; failure → its own reason). No loop: a second failure just bubbles up. When no cookie source is configured the retry branch is never taken (the cookie-arg array is empty), preserving today's behaviour.

Considered and rejected: (1) always passing cookies — would change yt-dlp's request shape for every URL, risking side effects and ignoring the user's implicit intent that cookies are a fallback; (2) retrying more than once or on any error — needless subprocess churn and a loop risk; (3) auto-exporting a browser cookie jar — out of scope, heavier, and the file/browser options already cover it. The cookie-as-argv approach is safe because `--ignore-config` is already passed (so a stray user yt-dlp config can't inject its own cookie source), and cookies are not treated as secrets stored in `AppSettings` — only a FILE PATH / browser NAME, consistent with the existing "preferences only, credentials to keychain" rule (§5). The path itself is not validated for existence at extractor time; yt-dlp reports a clear stderr reason on a bad path, which surfaces through the normal failure path.

UI is NOT changed in this pass (XAML untouched per task): the Video settings section needs two new bound controls — a TextBox for the cookie file path (label "Cookie file (Netscape format) — fallback for bot detection") bound to `VideoSettingsViewModel.YtDlpCookieFilePath`, and a TextBox (or validated dropdown of chrome/firefox/brave/edge/opera/safari) for the browser name (label "Or load cookies from browser") bound to `VideoSettingsViewModel.YtDlpCookieBrowser` — each persisted via `_settings.UpdateAsync(s => s with { YtDlpCookieFilePath = value })` mirroring the existing `VideoCaptureEnabled` toggle. A "Browse…" file picker for the path is a nice-to-have. Both should be disabled/greyed when `VideoCaptureEnabled` is off. This is documented for the frontend agent, not implemented here.

**Impact**: JustDownload.Core/Settings/AppSettings.cs (2 new nullable string props), JustDownload.Core/Settings/SettingsSerializer.cs (2 keys + ToStorage/FromStorage + using already present). JustDownload.Core/Media/YtDlp/YtDlpMediaExtractor.cs (BuildCookieArguments + IsBotDetectionReason helpers, RunAndMapAsync refactor, one-shot retry in TryExtractAsync, EventId 5 log). JustDownload.Tests/Media/YtDlpMediaExtractorTests.cs (6 new tests: no-cookie→no --cookies; cookie path + bot error→retry with --cookies; non-bot error→no retry; retry succeeds; retry also fails→one retry; browser variant). Full `dotnet build -c Release` 0 warn/0 err; YtDlpMediaExtractorTests 26/26 green.

## Extension: instagram.com routes through extraction, mirroring the x.com/twitter.com fix
**When**: 2026-08-14

User-reported bug (TASK-242): clicking the extension icon on `https://www.instagram.com/reels/Dbnng9EBBfr/` populated the New Download dialog with a `fbcdn.net` CDN URL carrying `bytestart=15876&byteend=30194` — a ~14KB fragment, downloaded verbatim as if it were the whole video, producing an unplayable raw fMP4 (`moof`/`mfhd`/`traf`, no `ftyp`/`moov`). Root cause: `instagram.com` was not in `EXTRACTABLE_HOSTS` (extension/src/jdcore.js), so `isExtractablePage()` returned false and the icon sent the sniffer's raw buffered-chunk URL as a plain (non-extracted) download instead of routing through the app's extraction pipeline. Same failure shape as the x.com/twitter.com bug fixed in TASK-241, but here it's a URL-construction problem (Instagram's own `bytestart`/`byteend` byte-serving query params, not standard HTTP Range) rather than an HLS-parsing gap.

Fix, exactly mirroring TASK-241's pattern: `instagram.com` added to `EXTRACTABLE_HOSTS` (extension/src/jdcore.js:151-159) so the reel/post page URL is handed to the app's extractor pipeline instead of a guessed stream URL. There is no in-house Instagram extractor in `JustDownload.Core/Media/` (confirmed — only `Dash/`, `Facebook/`, `Hls/`, `YouTube/`, `YtDlp/`, `Streams/`, `Extraction/` exist), so on an app without yt-dlp enabled the page is declined by every extractor — which is why this relies on the same generic `fallbackUrl` mechanism TASK-241 built (verified host-agnostic before reuse: `MediaVariantPickerViewModel.HasUsableFallback`/`CanUseFallback` only checks the URL is a valid absolute http(s) URL differing from the one that just failed — no Twitter-specific logic anywhere in it, `background.js`, or `content.js`). No Core/C# changes were needed at all.

Also added `bytestart`/`byteend` to `CHUNK_PARAMS` in `normalizeMediaUrl()` (previously only `range`/`rn`/`rbuf`, YouTube-specific), so the fallback URL (offered via the picker's "Use the stream your browser detected" button) at least targets the full resource instead of the tiny buffered chunk the sniffer happened to see. **Explicitly unverified**: whether stripping these params actually makes `fbcdn.net` serve the complete file (vs. erroring, or still defaulting to some fixed small chunk) — this sandbox is DNS-blocked from `facebook.com`/`instagram.com` (confirmed earlier this session via the user's NextDNS config), so there is no way to test live from here. Needs verification on Sarfraz's machine. It only matters when the fallback button is used; the default path for instagram.com is now page-URL extraction (yt-dlp), same as x.com.

**Impact**: extension/src/jdcore.js (EXTRACTABLE_HOSTS gained `instagram.com`; CHUNK_PARAMS gained `bytestart`/`byteend`). extension/test/jdcore.test.js (2 new tests: isExtractablePage covers instagram.com; normalizeMediaUrl strips bytestart/byteend while keeping unrelated params). extension/test/content.test.js (2 new tests mirroring the x.com pair: reel page routes through extraction; hand-off carries the sniffed CDN URL as fallbackUrl). All 78 tests green (`node --test` from extension/, baseline was 74 after TASK-241). `npm run build` verified byte-identical `jdcore.js` across dist/chrome, dist/edge, dist/firefox. No Core/.cs files touched — this was purely an extension-side routing fix, exactly like TASK-241.
