# Build output — regenerated by `npm run build` / `npm run package`
/dist/

# Note: src/icons/*.png are the real brand icons (generated from src/icon.svg by
# `npm run gen-icons`) and ARE committed so the unpacked extension loads as-is.

# Node
/node_modules/
*.log
