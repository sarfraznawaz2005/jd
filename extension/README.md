# JustDownload Browser Extension

MV3 browser extension that hands downloads off to the desktop app via the
Native Messaging Host (`JustDownload.NativeHost`), per locked decision **D8**.

This folder is a placeholder created during solution scaffolding (TASK-012).
The actual extension (manifest, background service worker, content scripts) is
implemented in a later task.
